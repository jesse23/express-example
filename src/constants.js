// constants
const APP_NAME = 'express-example';
const ITEM_APP = 'Item';

// Paths 
const PATH_ITEM = '/items';

// Parameters
const PARAM_UID = 'uid';

// Misc
const DEFAULT_PORT = 3000;

module.exports = {
  APP_NAME,
  ITEM_APP,
  PATH_ITEM,
  PARAM_UID,
  DEFAULT_PORT
};
